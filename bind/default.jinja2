# {{ salt['pillar.get']('message_do_not_modify') }}
{# run resolvconf? #}
RESOLVCONF=no

{# startup options for the server #}
OPTIONS="-u bind{% if not salt['monitoring.data']()['ip_addrs6']['public'] %} -4{% endif %}"
