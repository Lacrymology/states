# {{ pillar['message_do_not_modify'] }}

production:
  adapter: postgresql
  encoding: unicode
  database: gitlab
  pool: 10
  username: {{ salt['pillar.get']('gitlab:database:username', 'gitlab') }}
  password: "{{ salt['password.pillar']('gitlab:database:password', 10) }}"
  host: {{ salt['pillar.get']('gitlab:database:host', 'localhost') }}
  port: {{ salt['pillar.get']('gitlab:database:port', '5432') }}
  {#- socket: /tmp/postgresql.sock #}
