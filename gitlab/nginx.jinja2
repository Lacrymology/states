upstream gitlab {
    server unix:///var/lib/uwsgi/gitlab.sock;
}

server {
    listen 80;


    server_name localhost;
    root {{ root_dir }}/public;

    location / {
        gzip off;
        uwsgi_modifier1 7;
        include uwsgi_params;
        uwsgi_pass gitlab;
    }
}

