# {{ pillar['message_do_not_modify'] }}

respawn

start on filesystem or runlevel [2345]
stop on runlevel [!2345]
exec cd {{ web_dir }} && sudo -u git -H RAILS_ENV=production bundle exec rake sidekiq:start
