The global ``smtp`` :doc:`/doc/pillar` can be overridden for this particular
formula.

For details on its format, please see ``smtp`` section in :doc:`/doc/pillar`.

Default: uses global SMTP config (``False``).
