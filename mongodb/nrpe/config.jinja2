{#-
Use of this source code is governed by a BSD license that can be found
in the doc/license.rst file.

Author: Bruno Clermont <bruno@robotinfra.com>
Maintainer: Van Pham Diep <favadi@robotinfra.com>
-#}
{%- set timeout = 30 -%}
mongodb_http:
  description: MongoDB HTTP Interface
  command: /usr/lib/nagios/plugins/check_http -t {{ timeout }} -I 127.0.0.1 -p 28017{%- if salt['pillar.get']('debug', False) %} -v{%- endif %}
  timeout: {{ timeout }}
  dependencies:
    - mongodb_http_port

mongodb_port:
  description: MongoDB Port
  command: /usr/lib/nagios/plugins/check_tcp -H 127.0.0.1 -p 27017 -t {{ timeout }}
  timeout: {{ timeout }}
  dependencies:
    - mongodb_procs

mongodb_procs:
  description: MongoDB Daemon
  command: '/usr/lib/nagios/plugins/check_procs -c 1:1 -C mongod -u mongodb'

mongodb_http_port:
  description: MongoDB HTTP Port
  command: /usr/lib/nagios/plugins/check_tcp -H 127.0.0.1 -p 28017 -t {{ timeout }}
  timeout: {{ timeout }}
  dependencies:
    - mongodb_procs
