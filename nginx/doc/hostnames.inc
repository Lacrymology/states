List of `HTTP hostname <https://en.wikipedia.org/wiki/Hostname>`__ to reach the
webapp.

.. warning::

  The formula won't create automatically the
  `DNS <http://en.wikipedia.org/wiki/Dns>`__ entries automatically.

  The role is the place to configure DNS.
  Or it can be done manually.
