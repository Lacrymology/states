salt_cloud_configurations_check:
  description: Salt-Cloud Image Monitor
  check_interval: {{ 24 * 60 }} {# 1 day #}
  
  arguments:
    timeout: {{ 15 * 60 }}
    {# These two next paramters are optional. The present values are the default #}
    {# profile_file: /etc/salt/cloud.profiles #}{# pass to CLI as --profile-file #}
    {# providers_file: /etc/salt/cloud.providers #}{# pass to CLI as --providers-file #}