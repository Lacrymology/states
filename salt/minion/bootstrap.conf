# this configuration is only used to bootstrap a salt-master
# or to perform integration tests.

log_level: debug
file_client: local

file_roots:
   base:
     - /root/salt/states

pillar_roots:
  base:
    - /root/salt/pillar
