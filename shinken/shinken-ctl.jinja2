{#- Usage of this is governed by a license that can be found in doc/license.rst -#}

#!/bin/bash
# {{ salt['pillar.get']('message_do_not_modify') }}

{%- for role in ('broker', 'arbiter', 'reactionner', 'poller', 'scheduler', 'receiver') %}
if [ -r /etc/init/shinken-{{ role }}.conf ]; then
    $1 shinken-{{ role }}
fi
{%- endfor -%}
