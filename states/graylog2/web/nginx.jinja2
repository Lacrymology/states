# {{ pillar['message_do_not_modify'] }}

server {
    listen 80;
    server_name {{ pillar['graylog2']['web']['hostname'] }};
    location / {
        proxy_pass http://localhost:8000;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
