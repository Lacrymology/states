# {{ pillar['message_do_not_modify'] }}
# ubuntu upstart script for sentry

start on filesystem or runlevel [2345]
stop on runlevel [!2345]

respawn
respawn limit 10 5
umask 022

exec sudo -u nobody -g nogroup /usr/local/sentry/bin/sentry --config=/etc/sentry.conf.py start
